CREATE TABLE CM_LTR_HEAD (
	LTR_HEAD_SYS_NUM INTEGER,
	TITLE_TXT TEXT(256) NOT NULL,
	EFF_START_DT INTEGER NOT NULL,
	EFF_END_DT INTEGER,
	CREATED_BY TEXT(25) NOT NULL DEFAULT "SYSTEM",
	LAST_MODIFIED_BY TEXT(25) NOT NULL DEFAULT "SYSTEM",
	CREATED_DT INTEGER,	
	LAST_MODIFIED_DT INTEGER,
	LOCK_NUM INTEGER
);

CREATE TABLE CM_LTR (
	LTR_SYS_NUM NUMERIC NOT NULL,
	LTR_HEAD_SYS_NUM INTEGER,
	LTR_NUM TEXT(20) NOT NULL,
	ADDRESSEE_TYP INTEGER NOT NULL DEFAULT 1,  
	TITLE_TXT TEXT(256) NOT NULL,
	VERSION_NUM INTEGER NOT NULL,
	EFF_START_DT INTEGER NOT NULL,
	EFF_END_DT INTEGER,
	CREATED_BY TEXT(25) NOT NULL DEFAULT "SYSTEM",
	LAST_MODIFIED_BY TEXT(25) NOT NULL DEFAULT "SYSTEM",
	CREATED_DT INTEGER,	
	LAST_MODIFIED_DT INTEGER,
	LOCK_NUM INTEGER
);

# ADDRESSESS_TYP:
# 1: Got Recipient Name, and Address
# 2: No Recipient Name, but got Address